# plugin-manager 单包双面：同一入口既是 host 插件（网关）也是浏览器 UI
# （包声明 dsh.client，client-modules 把该行发布为浏览器 roster）。
# 单一 npm 包不再使用 file: 子包依赖，因此支持从 git URL 安装。
- insert:
    - id: plugin-manager
      name: '@xianminglf/plugin-manager'
